from setuptools import setup


if __name__ == '__main__':
    setup(
        name='ZPGEngine',
        packages=['core'],
        package_dir={'': 'src'},
        version='0.0.1',
        author='sir Max',
        include_package_data=True,
    )
